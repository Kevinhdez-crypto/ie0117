//codigo principal
//Kevin Hernandez Umaña. C33773
/*
En este programa, lo que se pretende es hacer una calculadora,
el cual recibe como parametros dos numeros enteros en cada funcion. Se podrà multiplicar, sumar,
restar, sacar el modulo de un numero y demas.
*/
#include <stdio.h>
#include "cabecera.h"

int main(){
    suma(5,4); //En este caso se llama la funcion suma y se recibe dos parametros enteros

    resta(10,6);//En este caso se llama la funcion resta y se recibe dos parametros enteros

    multiplicacion(5,1);//En este caso se llama la funcion multiplicacion y se recibe dos parametros enteros

    division(4,2);//En este caso se llama la funcion division y se recibe dos parametros enteros

    tipo(2,7);//En este caso se llama la funcion tipo y se recibe dos parametros enteros

    modulo(15,5);//En este caso se llama la funcion modulo y se recibe dos parametros enteros

    potencia(10,2);//En este caso se llama la funcion potencia y se recibe dos parametros enteros
}